<Application x:Class="Pract15.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="BackgroundColor">#dfeaeb</Color>
            <Color x:Key="PrimaryColor">#00C4B4</Color>
            <Color x:Key="SecondaryColor">#00b4c4</Color>
            <Color x:Key="LowStockColor">#ffd129</Color>
            <Color x:Key="TextColor">#333333</Color>
            <Color x:Key="WhiteColor">#FFFFFF</Color>

            <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
            <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
            <SolidColorBrush x:Key="LowStockBrush" Color="{StaticResource LowStockColor}"/>
            <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
            <SolidColorBrush x:Key="WhiteBrush" Color="{StaticResource WhiteColor}"/>

            <Style TargetType="TextBox">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="5"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>
            <Style x:Key="DeleteButton1" TargetType="Button">
                <Setter Property="Background" Value="#FF5252"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
           >
                                <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF3333"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#CC0000"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <Style TargetType="Button">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            
                <Style x:Key="ValidatingPasswordBoxStyle" TargetType="PasswordBox">
                    <Setter Property="Validation.ErrorTemplate">
                        <Setter.Value>
                            <ControlTemplate>
                                <StackPanel>
                                    <Border BorderBrush="Red" BorderThickness="2">
                                        <AdornedElementPlaceholder x:Name="placeholder"/>
                                    </Border>
                                    <TextBlock Text="{Binding ElementName=placeholder, 
                                                 Path=AdornedElement.(Validation.Errors)[0].ErrorContent}" 
                                   Foreground="Red" 
                                   FontSize="12"
                                   Margin="5,2,0,0"/>
                                </StackPanel>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            
            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="5"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>

            <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="#FF5252"/>
            </Style>
            <Style x:Key="AddButton" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Margin" Value="2"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="12"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="#009080"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="Text" TargetType="TextBox">
                <Setter Property="Margin" Value="0,0,0,10"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BackgroundBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Height" Value="40"/>
            </Style>
            <Style x:Key="LowStockProductStyle" TargetType="Border">
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="Margin" Value="0,2"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Stock}" Value="9">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="8">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="7">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="6">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="5">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="4">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="3">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="2">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="1">
                        <Setter Property="BorderBrush" Value="{StaticResource LowStockBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="0">
                        <Setter Property="BorderBrush" Value="Red"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="LowStockTextStyle" TargetType="Run">
                <Setter Property="Text" Value=""/>
                <Setter Property="Foreground" Value="Red"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Stock}" Value="0">
                        <Setter Property="Text" Value=" (Нет в наличии!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="1">
                        <Setter Property="Text" Value=" (Остался 1!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="2">
                        <Setter Property="Text" Value=" (Осталось 2!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="3">
                        <Setter Property="Text" Value=" (Осталось 3!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="4">
                        <Setter Property="Text" Value=" (Осталось 4!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="5">
                        <Setter Property="Text" Value=" (Осталось 5!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="6">
                        <Setter Property="Text" Value=" (Осталось 6!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="7">
                        <Setter Property="Text" Value=" (Осталось 7!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="8">
                        <Setter Property="Text" Value=" (Осталось 8!)"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Stock}" Value="9">
                        <Setter Property="Text" Value=" (Осталось 9!)"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ValidatingTextBoxStyle" TargetType="TextBox">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="5"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Height" Value="40"/>

                <Style.Triggers>
                    <Trigger Property="Validation.HasError" Value="True">
                        <Setter Property="BorderBrush" Value="Red"/>
                        <Setter Property="BorderThickness" Value="2"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ValidatingComboBoxStyle" TargetType="ComboBox">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="BorderBrush" Value="#CCCCCC"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="5"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Validation.ErrorTemplate">
                    <Setter.Value>
                        <ControlTemplate>
                            <StackPanel>
                                <Border BorderBrush="Red" 
                            BorderThickness="1" 
                            CornerRadius="3"
                            Background="White">
                                    <AdornedElementPlaceholder/>
                                </Border>
                                <TextBlock Text="{Binding [0].ErrorContent}" 
                               Foreground="Red" 
                               FontSize="12"
                               Margin="0,5,0,0"/>
                            </StackPanel>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="Validation.HasError" Value="true">
                        <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                        <Setter Property="BorderBrush" Value="Red"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="MaterialDesignComboBox" TargetType="ComboBox">
                <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BackgroundBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Height" Value="40"/>
            </Style>
            
            
        </ResourceDictionary>
    </Application.Resources>
</Application>